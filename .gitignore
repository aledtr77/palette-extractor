# Comments in English, like the rest of the repo. Entries that never matched
# anything here are gone: build/, images/cache/, secrets/, *.tmp and *.cache
# came from another project's file and only made this one harder to read.

# build / dependencies
node_modules/
dist/

# env / secrets
.env
.env.local
*.pem

# editor / OS
.vscode/
.DS_Store

# logs
*.log
npm-debug.log*
