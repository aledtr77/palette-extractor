{
  "name": "palette-extractor",
  "version": "1.0.0",
  "private": true,
  "description": "Pulls a colour palette out of an image entirely in the browser, and turns it into UI roles, WCAG contrast ratios, CSS custom properties and JSON",
  "license": "MIT",
  "author": "Alessandro Di Terlizzi",
  "homepage": "https://palette-extractor-7hm.pages.dev",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aledtr77/palette-extractor.git"
  },
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "release": "bash scripts/release.sh",
    "badge": "node scripts/sync-test-badge.mjs",
    "badge:check": "node scripts/sync-test-badge.mjs --check"
  },
  "dependencies": {
    "lucide": "^0.468.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "globals": "^17.9.0",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
